{
  "devDependencies": {
    "husky": "^7.0.1",
    "lint-staged": "^11.0.1",
    "prettier": "^2.3.2"
  },
  "scripts": {
    "prepare": "husky install",
    "prettier": "prettier --write \"**/*.{md,json}\""
  },
  "lint-staged": {
    "*.{md,json}": "prettier --write"
  }
}
